# frozen_string_literal: true

# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!
#
# This key is used during compilation of any static assets
# (e.g. Docker, etc.) in your application. If you change this key,
# existing signed cookies will become invalid.
#
# If your app is using encrypted credentials, set secret_key_base there.
# secret_key_base: some_random_string

# You can use `rails secret` to generate a secure secret key.

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# Use the secret_key_base for the master key.
#
# This secret is used for:
# - Signed cookies
# - Encrypted cookies
# - Session cookies (if using cookie-based sessions)
#
# If you're using encrypted credentials, set this in config/credentials.yml.enc:
# secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
#
# Otherwise, set it here:
secret_key_base: ENV.fetch("SECRET_KEY_BASE") { SecureRandom.hex(64) }
