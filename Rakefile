# frozen_string_literal: true

# This is the standard Rails Rakefile that loads the application.
require_relative "config/boot"
require "rails/all"
require "rake"

# Load the Rails application to register db/rake tasks
require_relative "config/application"
Rails.application.load_tasks
